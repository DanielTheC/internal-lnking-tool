# Dependencies
node_modules/

# Next.js
.next/
out/

# Env / local
.env
.env*.local

# Local incremental crawl cache (ETag / Last-Modified snapshots)
.cache/

# Encrypted GSC OAuth tokens (when not using Redis)
.data/

# Debug / OS
npm-debug.log*
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/

# Large / generated data (add filenames here if push is rejected for size)
all_inlinks.csv
*.zip
